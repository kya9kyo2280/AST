
<table class="table table-striped">

<thead>
    <tr>
      <th>image</th>
      <th>id</th>
      <th> name</th>
      <th> mail</th>
    </tr>
  </thead>
  <tbody>
    <% @users.each do |u| %>
    <tr>
      <td>
   <div >
    <%= attachment_image_tag u, :profile_image,　format: 'jpeg', fallback: "no_image.jpg", size: "50x50" %>
   </div>
</div></td>
  </div>
     <td><%= u.id %></td>
     <td><%= link_to u.name, admin_user_path(u.id) %></td>
     <td><%= u.email %></td>

    </tr>
    <% end %>
  </tbody>
</table>